<h1><%= @article.title %></h1>

<p><%= @article.body %></p>
<br>
<div>
  <%= link_to "Edit", edit_article_path(@article) %>
  <span> | </span>
  <%= link_to "Destroy", article_path(@article), method: :delete,
    data: { confirm: 'Pode deletar mesmo?' } %>
  <span> | </span>
  <%= link_to "Index" %>
</div>

<h2>Comments made:</h2>
<p>
  <%= @article.comments.public_count %> public comments made
</p>
<%= render @article.comments %>
<%# @article.comments.each do |comment| %>
<%# end %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>